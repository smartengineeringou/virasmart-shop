# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env*.local

# Local-only pricing / source data — never serve or commit.
# Non-served location (outside public/) used by scripts for one-off ingest.
lib/data/_sources/

# Common ignores
node_modules
.next/
.DS_Store
.vercel

# Next.js / TypeScript autogenerated artefacts — checked in by v0 initial
# commit but churn on every build. Ignore now so git state stays clean.
next-env.d.ts
tsconfig.tsbuildinfo
